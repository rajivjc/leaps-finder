# Local scanner runs. Copy to scanner/.env — it is git-ignored.
# In CI these come from GitHub Actions secrets, never from a file.
#
# The service key bypasses RLS. Treat it like a password; this repo is public.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
